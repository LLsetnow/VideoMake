# 绝区零 · B站保底参考 · 横屏 30 秒

## 任务

- 参考视频：`BV14dfBBvEAe`，只离线分析前 30 秒的节奏、镜头关系和视觉语言，不上传到 H3/ComfyUI。
- 目标：16:9 横屏、30 秒、24 fps 的原创动漫音乐视频。
- 角色：千夏、南宫羽、爱芮，按 `docs/绝区零.md` 使用独立身份参考图。
- 视觉参考：`<Picture 1–3>` 为六分街、厄匹斯港、沙滩场景；`<Picture 4–6>` 为三位角色身份图。
- 音频：从参考视频前 30 秒抽出的音频，分为两个 15 秒片段；每段使用 `lock_source`，完整复制为该段最终音轨。

## 目录

- `00_参考/`：下载的参考视频、联系表和抽取的 30 秒音频；不作为工作流视频输入。
- `1-15秒/`：第一段提示词、工作流和输出。
- `16-30秒/`：第二段提示词、工作流和输出。

## 生成策略

两段各 15 秒，使用 MiniMax H3 Ref2VA；每段均为 6 张独立图片 + 1 条 15 秒音频。工作流中的 `MiniMaxH3AudioConditioningT8` 设置为 `task_type=Ref2VA`、`audio_mode=lock_source`、`16:9`、`1.0 MP`，不包含视频加载节点。

## 假设

“次参考音频”按项目现有约定解释为：参考视频前 30 秒抽出的独立音频轨，并将其完整锁定到最终成片。未加入对白或角色音色参考，因为本片不要求新生成对白/歌声。
