# videoRef 目录规则

本目录只保存用于研究风格、动作、镜头或效果的参考视频。

- 默认不把参考视频当最终素材，也不自动上传到 H3 工作流；只有任务明确要求视频参考时才添加对应节点并在提示词中声明用途。
- 可通过 `opc bili`、`opc douyin` 或 `opc x` 下载；下载完成后检查文件完整性和可读性，再归档到这里。
- 不覆盖用户已有参考视频，不把 cookies 或其他凭据复制到此目录。

详细规则和命令见 [`docs/media-assets.md`](../docs/media-assets.md)。
